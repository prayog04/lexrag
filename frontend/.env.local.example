# Copy to .env.local. Point this at your FastAPI backend — the docker-compose
# service on localhost while developing, or your deployed Render/Fly URL once
# you've deployed it (see the root README).
NEXT_PUBLIC_API_URL=http://localhost:8000
